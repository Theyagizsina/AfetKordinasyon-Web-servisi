import axios from 'axios';



export const UpdateCampaignInvestment = async (campaignUrl: string, platform: string) => {

    /**const endpoint = "https://api2.kitlefonlama.market/campaign/add"
    const res = await axios.get(endpoint, {campaignUrl: campaignUrl, platform:platform}); */
    return "ok"
}